return {
    cmd = { "ty", "server" },
    filetypes = { "python" },
    -- root_dir = function()
    --     local root = vim.fs.root(0, { "pyproject.toml", "requirements.txt" })
    --     if root == "." then
    --         return "./."
    --     end
    --     return root
    -- end
}
